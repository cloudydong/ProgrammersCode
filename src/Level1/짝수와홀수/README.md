> 삼항  
```
public String solution(int num) {
        return (num%2==0) ? "Even" : "Odd";
    }
```
> 일반  
```
public String solution(int num) {
        if(num%2==0)
            return "Even";
        else
            return "Odd";
    }
```